"""
This is a first thing
and here is another
"""
type X implements A & B @something @other(A: true, B: "b", C: 3.14) {
  variable(): Date!
  "other is cool"

  other(xyz: String! = "abc" @test(x: 1), "This is some field" ooo: Date): Atype
}

scalar Date

interface NamedEntity {
  name: String
}

union SearchResult = Photo | Person

enum Direction {
  NORTH
  """
  This is the east, obviously
  """
  EAST
  "The south is here" SOUTH
  WEST
}

directive @cacheControl(
  maxAge: Int
  scope: CacheControlScope
) on FIELD_DEFINITION | OBJECT | INTERFACE

input Point2D {
  x: Float
  y: Float
}

schema {
    query: Query
    mutation: Mutation
}

